import { RedisConnection } from 'bullmq';
import { version as packageVersion } from '../version';
import * as scripts from '../scripts';
export class RedisConnectionPro extends RedisConnection {
    constructor(opts, shared = false, blocking = true, skipVersionCheck = false) {
        super(opts, shared, blocking, skipVersionCheck);
        this.packageVersion = packageVersion;
    }
    async loadCommands(version, providedScripts) {
        const finalScripts = providedScripts || scripts;
        return super.loadCommands(version, finalScripts);
    }
}
//# sourceMappingURL=redis-connection-pro.js.map