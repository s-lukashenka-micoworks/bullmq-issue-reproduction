--[[
  Delete jobs belonging to a given group.

  Input:
      KEYS[1] key prefix
      KEYS[2] groupkey
      KEYS[3] groupsKey

      -- Arguments
      ARGV[1] group id
      ARGV[2] limit amount of jobs to delete
]]

local rcall = redis.call

-- Includes
--- @include "includes/deleteGroup"

local removedJobs, jobsLeft = deleteGroup(KEYS[3], KEYS[2], ARGV[1], KEYS[1], ARGV[2])
return jobsLeft
