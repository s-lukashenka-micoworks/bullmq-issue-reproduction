import { ConnectionOptions, RedisConnection, RawCommand } from 'bullmq';
export declare class RedisConnectionPro extends RedisConnection {
    protected packageVersion: string;
    constructor(opts?: ConnectionOptions, shared?: boolean, blocking?: boolean, skipVersionCheck?: boolean);
    loadCommands(version: string, providedScripts?: Record<string, RawCommand>): Promise<void>;
}
